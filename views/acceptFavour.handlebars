<!doctype html>
<html>

<!-- Title of tab / app -->
<head>
	<title>Homepage</title>

	<!-- Bootstrap to stylesheet -->
	<link href = "/css/acceptFavour.css" rel = "stylesheet">

	<!-- Viewtype that prevents from zooming out when changing to phone -->
	<meta name = "viewport" content = "width=device-width, initial-scale=1.0">
</head>

<!-- Actual app -->
<body>
	<div align = "center">
	<div class = "container">

		<!-- Requesting Favor -->
		<div class = "main" id = "bulk">
			<div class = "favours" id = "{{id}}">
				<!-- Image -->
				<img src = "images/{{image}}" class = "image">
				<!-- Subject -->
				<p>{{name}}</p>
				<p>Time: {{time}}</p>
				<p>Location: {{location}}</p>
				<div class = "box" id = "description">
					<p>{{description}}</p>
				</div>
			</div>
		</div>

	</div>
	</div>

</body>

<!-- End of file -->
</html>
